import SwiftUI
import UIKit

/*struct AdBannerViewController: UIViewControllerRepresentable {
    func makeUIViewController(context: Context) -> UIViewController {
        let adView = FBAdView(placementID: "f02310e6479b1c6ae758a990a8d610f9", adSize: kFBAdSizeHeight50Banner, rootViewController: UIApplication.shared.windows.first?.rootViewController
                              )
        adView.delegate = context.coordinator
        return UIViewController()
    }

    func updateUIViewController(_ uiViewController: UIViewController, context: Context) {
        // No es necesario realizar actualizaciones aquí
    }

    func makeCoordinator() -> Coordinator {
        Coordinator()
    }

    class Coordinator: NSObject, FBAdViewDelegate {
        // Implementa los métodos del delegado si es necesario
    }
}
*/
